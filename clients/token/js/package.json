{
	"name": "moq-smoke-token",
	"private": true,
	"type": "module",
	"description": "Installs the published @moq/auth package so token.sh can drive its CLI under node and bun.",
	"dependencies": {
		"@moq/auth": "latest"
	}
}
